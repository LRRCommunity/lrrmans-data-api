# lrrmans-data-api
Saves data from Project Cars 2 to InfluxDB instance

Build project with `gradlew clean build`

Run project with VM arguments `-Dspring.profiles.active=local -Dinfluxdb.password=YOUR-PASSWORD`